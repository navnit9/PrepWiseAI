[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

# SPA fallback so client-side routes work on direct hits / refreshes
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
